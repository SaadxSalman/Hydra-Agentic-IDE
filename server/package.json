{
  "name": "hydra-server",
  "version": "1.0.0",
  "private": true,
  "description": "Hydra-IDE Router & Swarm Dispatcher — orchestrates the 100-agent LFM swarm over a DAG task graph.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "node src/index.ts",
    "dev": "node --watch src/index.ts --dev",
    "build": "echo \"hydra-server is run from source via native Node TS execution (no build step required)\"",
    "test": "node --test --experimental-test-coverage \"test/*.test.ts\""
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/ws": "^8.18.1",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=22.6"
  }
}
